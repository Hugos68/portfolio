/*place global styles here */
html, body { 
    @apply h-full overflow-hidden; 
}

:root {
    --header-height: 5.125rem;
}

.page {
    @apply container mx-auto min-h-[calc(100vh-var(--header-height))];
}