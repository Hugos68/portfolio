/*place global styles here */
html, body { @apply h-full overflow-hidden; }

:root {
    --header-height: 5rem;
}

* {
    @apply transition-colors;
}

.page {
    @apply px-8 container mx-auto min-h-[calc(100vh-var(--header-height))];
}